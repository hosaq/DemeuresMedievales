<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta content="width=device-width, initial-scale=1.0" name="viewport">

        <link crossorigin="anonymous" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" rel="stylesheet">

        <link href="/css/bootstraplitera.min.css" rel="stylesheet" type="text/css">
        <link href="/css/fontawesome/all.css" rel="stylesheet" type="text/css">
        <link href="/css/base.css" rel="stylesheet" type="text/css">
        <style>
            body {
                display: flex;
                justify-content: center;
                height: 100vh;
            }
            .card {
                margin-top: 10%;
            }
        </style>

        <title>
            Gîtes en pays médiéval - Administation -
        </title>
    </head>

    <body>
        <div class="col-md-4 mt-10">
            <div class="bg-light py-3 px-3">
                <h4>
                    <i class="fas fa-lock"></i>
                    Connexion à l'administration</h4>
                {% if hasError %}
                    <div class="alert alert-danger">
                        Une erreur est survenue, l'adresse mail ou le mot de
                                                                    passe sont invalides.
                    </div>
                {% endif %}
                <form action="{{path('admin_connexion')}}" method="post">
                    <div class="form-group">
                        <label class="" for="email">Email</label>
                        <input class="form-control" id="email" name="_username" placeholder="Adresse mail..." type="text" value="{{username}}" required>
                    </div>
                    <div class="form-group">
                        <label for="password">Mot de passe</label>
                        <input class="form-control" id="password" name="_password" placeholder="Mot de passe..." required type="password">
                    </div>
                    <div class="form-group">
                        <button class="btn btn-danger btn-arrondi mr-5" type="submit">
                            <i class="fas fa-lock-open"></i>
                            Connexion
                        </button>
                        <span>
                            ou
                        </span>
                        <a href="{{path('homepage')}}" class="btn btn-success btn-arrondi ml-5">
                            <i class="fas fa-arrow-alt-circle-left"></i>
                            Retour à l'accueil</a>
                    </div>
                </form>
            </div>
        </div>
    </body>
</html>


