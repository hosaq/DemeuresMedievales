{% extends 'base.html.twig' %}

{% block title %}Tag index{% endblock %}

{% block body %}
    <h1>Tag index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Terme</th>
                <th>Slug</th>
                <th>Catégorie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tag in tags %}
            <tr>
                <td>{{ tag.id }}</td>
                <td>{{ tag.terme }}</td>
                <td>{{ tag.slug }}</td>
                 <td>{{ tag.genreinteret.nom }}</td>
                <td>
                    <a href="{{ path('tag_show', {'slug': tag.slug}) }}">show</a>
                    <a href="{{ path('modifie_tag', {'slug': tag.slug}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('nouveau_tag') }}">Create new</a>
{% endblock %}
