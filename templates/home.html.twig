{% extends 'base.html.twig' %}
{%block stylesheets %}
    
{% endblock %}
{% block body %}

<div class="container">
    <div class="jumbotron mt-3">
        <h1 class="display-3">Bienvenue sur &laquo Gîtes en Pays Médiéval &raquo !</h1>
        <p class="lead">Nous vous permettons, Ô vous de ces temps reculés, de disposer  
                    rapidement d'un site Internet afin de mettre votre grange, votre ferme ou votre chateau en
                    location et d'en retirer quelques sous !</p>
        <hr class="my-4">
        <p class="lead">Vous, courageux voyageur, parcourrez notre site et trouvez un logement pour votre 
                    prochaine quête !</p>
        <p class="lead">
            <a class="btn btn-primary btn-lg" href="{{path('les_annonces')}}" role="button">Voir nos annonces</a>
        </p>
    </div>

    <h2>Nos appartements récents...</h2>
    <div class="row mt-5">
        {% for data in annonces %}
            {% include '/elements/carteimmo.html.twig' with {'bien':data.annonce} %}
        {% endfor %}
    </div>
    <h2 class="mt-5 mb-3"> Nos logeurs récents :</h2>
    
    <div class="row mt-5 ">
    {% for data in proprios %}
        {% set proprio=data.user %}
        <div class="col-6">
            <div class="card border-secondary mb-3">
            <div class="card-body">
            <h4 class="card-title">{{proprio.identite}} 
                <span class="ml-4 petit">
                    {% include 'elements/notation.html.twig' with {'note': data.notemoyenne } %}
                </span>
            </h4>
            <hr>
            <div class="card-text">
            <img src="{{proprio.avatar}}" alt="{{proprio.identite}}" class="ronde float-left mr-3">
            <p>{{proprio.introduction}}</p>
            <a href="{{path('membre_afficher',{'slug':proprio.slug})}}" class="btn btn-success float-right bdr">En savoir plus !</a>
            </div>
            </div>
            </div>
        </div>
    
       {% endfor %}
    </div>       
</div>


{% endblock %}
{% block javascripts %}{% endblock %}
